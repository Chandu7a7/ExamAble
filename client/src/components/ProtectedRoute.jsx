import React from "react";

// Placeholder protected route: just renders children for now.
// Later you can hook this into AuthContext and redirect if not authenticated.
const ProtectedRoute = ({ children }) => {
  return children;
};

export default ProtectedRoute;

